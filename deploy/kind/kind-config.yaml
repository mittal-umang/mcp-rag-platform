kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: mcp-rag
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30800   # nodePort for the agent, if exposed
        hostPort: 8000
